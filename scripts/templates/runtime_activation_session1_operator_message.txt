# Paste into Hermes as Session 1 user message (runtime activation).
# Full checklist lives on disk — do not paste deployment-handoff.md into chat.
#
# If you still hit context limits, use only the "MINIMAL" paragraph below.

MINIMAL:
Session 1 only. Read policy-root core/deployment-handoff.md from disk; run "## Runtime Activation Prompt" verification and preflight; output that section's Required output bullets. Paths: HERMES_HOME/.hermes.md. **Mandatory before Verification:** a top-level **Handoff** the operator will paste into Session 2 — **A. Outstanding tasks** (numbered; `1. None.` if none) and **B. Next session (Session 2)** (numbered **executable** steps: `POLICY_ROOT/core/governance/artifacts-and-archival-memory.md`, `POLICY_ROOT/core/agentic-company-deployment-pack.md`, confirm `WORKSPACE/operations/`). No prose-only substitute; skipping **Handoff** = incomplete. Then **Verification** as in STANDARD step 6 (abbreviated if needed).

STANDARD:
You are the Chief Orchestrator for Session 1 (runtime activation only).

1. Open HERMES_HOME/.hermes.md and note POLICY_ROOT and workspace paths (do not paste them back at length).
2. Using file tools, read POLICY_ROOT/core/deployment-handoff.md starting at "## Runtime Activation Prompt" — read in sections if the file is large. Do not ask the operator to paste this document.
3. Follow "Hermes runtime discipline" and the activation checklist through verification, preflight, and security classification for this session only. Do not stand up directors or project leads yet unless the checklist explicitly requires naming them.
4. End with the "Required output" bullet list from that section and one line: next session = Session 2 from the "Session-by-session prompt order" table in the same file.
5. **Handoff (required)** — Before **Verification**, output a section titled **Handoff** with two bold subsections on their own lines, each followed by a **numbered** list (not one paragraph): **A. Outstanding tasks** (blockers, deferrals, human steps; `1. None.` if nothing open). **B. Next session (Session 2)** — numbered **executable** tasks for the next fresh-context chat (exact `POLICY_ROOT/...` paths: `core/governance/artifacts-and-archival-memory.md`, `core/agentic-company-deployment-pack.md`; confirm `WORKSPACE/operations/` layout). The operator copies **Handoff** into Session 2’s **Operator carry-in**; skipping **Handoff** or omitting numbered A/B is an **incomplete** Session 1.
6. End with a **Verification** section: absolute HERMES_HOME + profile name; read `.hermes.md` and print policy + workspace absolute paths (one line each); confirm skip_context_files is not disabling injection; list context files you read this session (`.hermes.md`, `WORKSPACE/BOOTSTRAP.md`, `WORKSPACE/AGENTS.md`, `SOUL.md`) with **verbatim first line or first heading** from disk for each; bullet list this session’s policy paths read + one-line purpose; name three topic phrases from the handoff/workspace rules you are following (do not dump the full system prompt).
